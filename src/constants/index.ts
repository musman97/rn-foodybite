import { JosefinSansRegular, JosefinSansBold } from "./fonts";
import { black, white, screenPrimaryBg, primary } from "./colors";
import { splashBg, splashLogo, logoWhite, authBg } from "./images";
import * as Icons from "@expo/vector-icons";

const Fonts = {
    bold: JosefinSansBold,
    regular: JosefinSansRegular,
};

const Colors = {
    black,
    white,
    screenPrimaryBg,
    primary,
};

const Images = {
    splashBg,
    splashLogo,
    authBg,
    logoWhite,
};

export { Fonts, Colors, Images, Icons };
